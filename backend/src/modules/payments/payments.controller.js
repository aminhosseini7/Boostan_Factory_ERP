const {z}=require('zod');const s=require('./payments.service');const schema=z.object({customerId:z.string().uuid(),amount:z.coerce.number().positive(),paymentMethod:z.string().min(1).max(50),paidAt:z.string().datetime().optional(),note:z.string().max(1000).optional().or(z.literal(''))});async function list(req,res){res.json(await s.list());}async function create(req,res){res.status(201).json(await s.create(schema.parse(req.body),req.user.id));}module.exports={list,create};
