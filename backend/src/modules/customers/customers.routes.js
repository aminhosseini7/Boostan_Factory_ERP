const router=require('express').Router();const auth=require('../../middleware/auth');const authorize=require('../../middleware/authorize');const ah=require('../../utils/asyncHandler');const c=require('./customers.controller');router.use(auth);router.get('/',ah(c.list));router.get('/:id/statement',authorize('MANAGER'),ah(c.statement));router.get('/:id',ah(c.get));router.post('/',ah(c.create));router.put('/:id',authorize('MANAGER'),ah(c.update));module.exports=router;
