# اصلاحات نسخه ۲.۸ — خلوت‌سازی فرم فروش و محدودسازی تقویم شمسی

این بسته فقط اصلاحات Frontend روی نسخه ۲.۷ است و هیچ Migration، تغییر دیتابیس یا Reset داده‌ای ندارد.

## تغییرات

- بازه انتخاب سال در فیلترهای تاریخ شمسی به **۱۴۰۵ تا ۱۴۱۰** محدود شد. تقویم نیز خارج از این بازه حرکت نمی‌کند.
- بخش اطلاعات راننده در فروش از ابتدا مخفی است و فقط با دکمه «اطلاعات راننده» باز می‌شود؛ این بخش فقط برای فروش نسیه نمایش داده می‌شود تا فرم فروش نقدی و کارت شلوغ نشود. اطلاعات راننده در فروش‌های غیرنسیه نیز به API ارسال نمی‌شود.
- توضیح «تاریخ و ساعت فروش به‌صورت خودکار ثبت می‌شود» از فرم فروش حذف شد.
- عنوان‌های تکراری «ثبت فروش» و «اقلام فروش» داخل فرم حذف شدند؛ عنوان اصلی صفحه کافی است.
- «افزودن یک قلم دیگر» به یک دکمه کوچک با علامت + تبدیل شد تا فضای فرم کمتر اشغال شود.
- عبارت «تعداد اقلام فروش» فقط زمانی نمایش داده می‌شود که بیش از یک قلم در فروش وجود داشته باشد.

## نصب

ZIP را روی همان پروژه شاخه `factory-v2-final` باز کنید و فایل‌های هم‌نام را جایگزین کنید. سپس در ریشه پروژه اجرا کنید:

```cmd
npm run build
```

اگر Build موفق بود:

```cmd
git add frontend/src/components/JalaliDateInput.jsx frontend/src/components/SaleEntry.jsx frontend/src/styles.css UPGRADE_V2_8_FA.md
git commit -m "Polish sale form and Jalali date range v2.8"
git push origin factory-v2-final
```

**نیازی به اجرای `cloud/deploy-supabase.bat`، Migration، Reset دیتابیس یا Deploy مجدد Edge Function نیست.**

## تست سریع

1. فیلتر تاریخ را باز کنید و مطمئن شوید فقط سال‌های ۱۴۰۵، ۱۴۰۶، ۱۴۰۷، ۱۴۰۸، ۱۴۰۹ و ۱۴۱۰ نمایش داده می‌شوند.
2. فروش نقدی را باز کنید: اطلاعات راننده نباید صفحه را شلوغ کند.
3. فروش نسیه را انتخاب کنید: دکمه «اطلاعات راننده» ظاهر شود و با یک کلیک فیلدهای راننده و پلاک باز شوند.
4. روی + یک‌بار بزنید؛ فقط یک قلم جدید اضافه شود. در حالت یک قلم، متن تعداد اقلام دیده نشود؛ بعد از اضافه‌شدن قلم دوم، تعداد ۲ نمایش داده شود.
