# آپدیت ERP بوستان — 2026-09-26

این بسته **مستقیماً بر اساس `Boostan_Factory_ERP_Final (4).zip`** ساخته شده است. فایل‌های قدیمی migration و قابلیت‌های فعلی دستکاری نشده‌اند؛ تغییر دیتابیس فقط در migration جدید `202609260001_sales_stock_and_cleaning_rotation.sql` قرار دارد.

## اصلاح ۱ — فروش بدون محدودیت موجودی دفتری

ثبت فروش و اصلاح فاکتور توسط مدیر دیگر به دلیل کمبود «موجودی ثبت‌شده» متوقف نمی‌شود. اگر تولید واقعی در شیفت انجام شده اما کانتر شیفت بعد هنوز ثبت نشده باشد، فروش ثبت می‌شود و موجودی دفتری می‌تواند موقتاً منفی شود. سایر کنترل‌های فروش شامل محصول فعال، تعداد مثبت، عدم تکرار محصول، نوع پرداخت، مبلغ نهایی، کنترل فروش مشابه و محدودیت اصلاح فاکتور مرجوع‌شده حفظ شده‌اند.

هشدار موجودی منفی موجود در بخش کنترل یکپارچگی نرم‌افزار حذف نشده است.

## اصلاح ۲ — نظافت چرخشی سه Zone

چرخه از **۵ مهر ۱۴۰۵ = 2026-09-27** شروع می‌شود و پیوسته ۳۰روزه است:

- ۱۰ روز اول: کامران = Zone 1، حسن = Zone 2، محمد = Zone 3
- ۱۰ روز دوم: کامران = Zone 2، حسن = Zone 3، محمد = Zone 1
- ۱۰ روز سوم: کامران = Zone 3، حسن = Zone 1، محمد = Zone 2
- سپس چرخه از ابتدا تکرار می‌شود.

Zone براساس `shift_date` و اپراتور شیفت در سمت دیتابیس محاسبه می‌شود؛ ساعت/تاریخ گوشی کارگر مبنا نیست. در پنل اپراتور، داخل «پایان شیفت و ثبت معیوب»، Zone مسئولیت نمایش داده می‌شود و کارگر می‌تواند تیک انجام نظافت را بزند. تیک اجباری نیست. فقط خود اپراتور همان شیفت می‌تواند نظافت را تأیید کند؛ بنابراین سبز شدن وضعیت به معنی تأیید اپراتور مربوط است.

## اصلاح ۳ — جدول تولید و شیفت‌ها

در هر دو جدول «گردش شیفت‌ها و کانتر دستگاه» و «تولید نهایی‌شده و اضافه‌شده به موجودی»:

- ستون «محصول» به «کد» تبدیل شده و فقط کد کالا نمایش داده می‌شود.
- داخل ستون «شیفت» فقط `صبح` / `عصر` / `شب` نمایش داده می‌شود.
- وضعیت نظافت به صورت جمع‌وجور نمایش داده می‌شود: `🟢` انجام‌شده، `🟠` انجام‌نشده. رکوردهای قبل از شروع چرخه `—` دارند.
- هر دو جدول اسکرول/Swipe افقی و عمودی دارند و سرستون هنگام اسکرول عمودی ثابت می‌ماند.

## فایل‌های تغییرکرده

- `frontend/src/pages/OperatorPanel.jsx`
- `frontend/src/pages/Production.jsx`
- `frontend/src/styles.css`
- `supabase/functions/erp-api/index.ts`
- migration جدید: `supabase/migrations/202609260001_sales_stock_and_cleaning_rotation.sql`

## ترتیب استقرار پیشنهادی

قبل از استقرار از دیتابیس backup قابل بازیابی بگیرید و `git status -sb` را ذخیره کنید.

در ریشه پروژه روی Windows CMD:

```bat
psql -X -h "aws-0-ap-southeast-2.pooler.supabase.com" -p 5432 -U "postgres.oinxpqbidpasaalauwlv" -d postgres -W -v ON_ERROR_STOP=1 -f "supabase\migrations\202609260001_sales_stock_and_cleaning_rotation.sql"
```

سپس Edge Function:

```bat
npx supabase functions deploy erp-api --project-ref oinxpqbidpasaalauwlv --use-api
```

و بعد build/test:

```bat
npm install
npm run build
npm test
```

پس از استقرار migration، فایل `VERIFY_UPDATE_20260926_READ_ONLY.sql` را می‌توان برای کنترل خواندنی اجرا کرد.

**توجه:** این بسته در این محیط روی دیتابیس فعال Supabase اجرا نشده و deploy نیز از طرف من انجام نشده است. قبل از اعلام موفقیت، خروجی اجرای migration، build و test باید بررسی شود.
