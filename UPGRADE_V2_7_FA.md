# اصلاحات نسخه ۲.۷ — اقلام فروش، پلاک ایرانی و تقویم شمسی

این بسته را فقط روی پروژه دارای نسخه ۲.۶ اعمال کنید. تغییرات صرفاً در Frontend هستند: هیچ SQL، Migration یا تغییری در داده‌های Supabase اجرا نمی‌شود.

## اصلاحات

- حذف listener سراسری تغییر اعداد در `frontend/src/main.jsx` که مستقیماً مقدار input کنترل‌شده توسط React را دست‌کاری می‌کرد و هنگام تایپ فارسی می‌توانست باعث ناهماهنگی فیلدهای پلاک شود. ورودی عددی موجود در فرم‌های دیگر (تلفن مشتری و تعداد ماه سرشکن هزینه) نیز به صورت محلی ارقام فارسی را مدیریت می‌کنند.
- هر قلم فروش دارای شناسه یکتاست؛ دکمه افزودن فقط یک قلم ایجاد می‌کند و حذف فقط همان یک ردیف را حذف می‌کند. تعداد اقلام کنار دکمه درج می‌شود تا «دو قلم در کل» از «دو قلم جدید» قابل تشخیص باشد.
- هر قسمت پلاک مستقل اصلاح می‌شود؛ تایپ حرف، ارقام سایر قسمت‌ها را پاک نمی‌کند. اعداد فارسی و عربی به اعداد استاندارد برای اعتبارسنجی تبدیل می‌شوند و انتقال نشانگر پس از تکمیل هر بخش، شامل ورودی دارای روش نگارش/ترکیب کاراکتر در موبایل، انجام می‌شود.
- در صفحه گزارش‌ها و دفتر کل عملیات، فیلتر تاریخ جلالی هم دارای انتخابگر جداگانه سال/ماه/روز و هم دارای تقویم دیداری شمسی با انتخاب مستقیم روز، جابه‌جایی ماه، «امروز» و پاک‌کردن است؛ تبدیل به تاریخ میلادی برای درخواست API فقط پس از انتخاب تاریخ کامل انجام می‌شود.

## استقرار

ZIP را در ریشه همان پروژه Git فعلی که روی شاخه `factory-v2-final` است باز کنید و فایل‌های هم‌نام را جایگزین کنید. در ریشه پروژه:

```cmd
npm run build
git status
```

اگر Build موفق بود، تنها فایل‌های این بسته را Stage کرده و Commit/Push کنید:

```cmd
git add frontend/src/main.jsx frontend/src/components/IranPlateInput.jsx frontend/src/components/JalaliDateInput.jsx frontend/src/components/SaleEntry.jsx frontend/src/utils/saleItems.js frontend/src/pages/Customers.jsx frontend/src/pages/Expenses.jsx frontend/src/styles.css UPGRADE_V2_7_FA.md
git commit -m "Fix Persian plate, sale lines and Jalali date picker v2.7"
git push origin factory-v2-final
```

بعد از سبزشدن GitHub Actions، سایت را روی موبایل کاملاً ببندید و دوباره باز کنید. اگر نسخه نصب‌شده PWA قبلی را نمایش می‌دهد، برنامه را تازه‌سازی و در صورت لزوم میان‌بر/نصب قبلی را حذف و مجدداً از سایت نصب کنید.

**نیازی به اجرای `cloud/deploy-supabase.bat`، Migration، Reset دیتابیس یا Deploy مجدد Edge Function نیست.**

## تست دستی پیش از شروع داده‌های واقعی

1. صفحه فروش را باز کنید؛ در ابتدا یک ردیف وجود داشته باشد؛ با یک کلیک روی «افزودن یک قلم دیگر» مجموع ردیف‌ها دو شود؛ حذف ردیف دوم باید ردیف اول را حفظ کند. این عمل را هم در حساب مدیر و هم در پنل اپراتور انجام دهید.
2. ابتدا ارقام فارسی «۳۶» در بخش اول، «۶۵۸» در بخش وسط، «۴۹» در بخش کد شهر و سپس حرف «ن» را وارد کنید. هر چهار قسمت باید باقی بمانند. بار دوم به‌ترتیب معمول از بخش اول به بعد وارد کنید و انتقال خودکار نشانگر را بررسی کنید.
3. در بخش گزارش‌ها از تقویم، تاریخ ۱۴۰۵/۰۶/۲۸ را انتخاب کنید؛ همچنین انتخاب سال/ماه/روز و پاک‌کردن تاریخ را آزمایش کنید.
4. روی موبایل واقعی (نوع کیبورد فارسی و مدل‌های اندروید و آیفون ممکن است رفتار IME متفاوتی داشته باشند) مورد ۲ را آزمایش کنید. تست نحو و منطق داده به‌تنهایی تأیید نهایی رفتار تمام کیبوردهای موبایل نیست.
