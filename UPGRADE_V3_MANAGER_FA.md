# بسته پیشنهادی v3 — کنترل‌های پنل مدیر بوستان

**این بسته یک اصلاح آمادهٔ بررسی است، نه نسخهٔ آزموده‌شدهٔ عملیاتی. آن را روی دیتابیس و کارخانهٔ فعال، بدون بکاپ تازه و آزمون در محیط جداگانه، اجرا نکنید.** فایل‌های قدیمی migration شامل دستورهای تخریبی هستند؛ فقط SQL جدید داخل همین بسته در نظر گرفته شده است.

## انجام‌شده در کد

- پنل مدیر: داشبورد دارای فیلتر تاریخ شمسی و شاخص فروش ناخالص/خالص، تولید، مانده بدهی و موجودی پایان بازه (موجودی بر اساس **قیمت فروش** نمایش داده می‌شود، نه بهای تمام‌شده).
- پنل مدیر: تحلیل جداگانهٔ هر سبد، تعداد و فروش بازه، **برآورد** بهای واحد/سود/حاشیه سود. تا زمانی که ارزش‌گذاری موجودی ابتدای دوره، ترکیب مواد، ضایعات بازیافتی و تخصیص قطعی هزینه‌ها اضافه و بررسی نشده، این مبلغ *بهای تمام‌شدهٔ واقعی حسابداری نیست*.
- پنل مدیر: محاسبهٔ موجودی برآوردی در شیفت فعال با کانتر ۷ رقمی و تعداد معیوب. برآورد به انبار و کانتر واقعی چیزی نمی‌افزاید.
- کنترل ورود کانتر شروع شیفت و محاسبه‌گر مدیر: دقیقاً هفت رقم (با حفظ صفرهای اول)؛ **چیدمان پنل اپراتور تغییری نکرده است**.
- پنل مدیر: مبلغ مجزای تخفیف تسویه هنگام وصول؛ از بدهی کم می‌شود، ولی جزو وجه نقد دریافتی نیست.
- پنل مدیر: امکان اصلاح فروش‌های فعال، حتی بعد از یک ساعت، با دلیل اجباری و تاریخچهٔ تغییرات؛ مرجوعی/ابطال‌شده از این مسیر قابل اصلاح نیست.
- کنترل سمت دیتابیس در ثبت فروش: ممانعت از تراکنش کاملاً مشابه در ده دقیقهٔ اخیر حتی با درخواست همزمان، با شناسهٔ یکتای درخواست برای جلوگیری از ارسال تکراری؛ مدیر می‌تواند برای دو فروش واقعاً مستقل با دلیل ثبت جداگانه کند.
- پنل مدیر: فعال‌سازی مجدد محصول غیرفعال؛ مخفی‌کردن شش گزینهٔ کم‌استفاده در «سایر امکانات مدیریت» و جداکنندهٔ کمرنگ.

## در این بسته انجام نشده / محدودیت مهم

**ثبت و همگام‌سازی امن اطلاعات هنگام قطعی چندروزهٔ اینترنت پیاده‌سازی نشده است.** فقط صفحات کش‌شدهٔ PWA ممکن است باز شوند، اما عملیات واقعی همچنان به اتصال به سرور نیاز دارد. تا آماده‌شدن گردش آفلاینِ آزموده‌شده، ثبت فروش/کانتر در زمان قطعی اینترنت را در دفتر کاغذی دارای شمارهٔ مسلسل، ساعت، اپراتور، نوع/تعداد سبد و مبلغ انجام دهید؛ پس از اتصال، یک نفر مسئول ورود کنترل‌شدهٔ اطلاعات شود و پیش از ثبت، فروش‌های همان بازه را بررسی کند. ورود همزمان از چند گوشی بدون سرور خطر دوباره‌کاری دارد.

**قیمت/سود به‌تفکیک محصول برآوردی است.** برای نمایش بهای تمام‌شدهٔ *قطعی* باید ارزش موجودی ابتدای دوره، بچ مواد اولیه، خرید ضایعات و راندمان آسیاب، زمان دستگاه و تخصیص حقوق/برق/تعمیرات تعریف و با دادهٔ واقعی صحت‌سنجی شود. تخفیف وصول تا زمانی که به اقلام فاکتور تخصیص داده نشده، در سود محصول منظور نمی‌شود.

**فاکتور دارای مرجوعی یا ابطال، یا ویرایشی که بدهی مشتری را منفی کند، از فرم اصلاح مستقیم رد می‌شود.** بعد از ویرایش اطلاعات تاریخی، گزارش بازه‌های قدیمی نیز بر اساس آخرین نسخهٔ فاکتور نمایش داده می‌شود؛ گزارش حسابرسی «همان‌طور که در تاریخ قدیم دیده می‌شد» نیازمند دفتر وقایع کامل‌تری است. محاسبه‌گر وسط شیفت فرض می‌کند از شروع شیفت فقط یک نوع محصول تولید شده و معیوب ثبت‌شدهٔ کاربر کامل است؛ مقدار نمایش‌داده‌شده **برآورد** است.

## ترتیب ایمن بررسی و انتشار

۱. در زمان توقف ثبت عملیات، از دیتابیس فعلی بکاپ جدید بگیرید و `pg_restore -l` را برای همان فایل اجرا کنید؛ بکاپ قدیمیِ قبل از پاک‌سازی، نسخهٔ فعلی اطلاعات نیست. بهتر است بازیابی روی دیتابیس آزمایشی مستقل نیز آزموده شود.

۲. فایل‌های ZIP را روی نسخهٔ v2.9 پوشهٔ اصلی Git شاخهٔ `factory-v2-final` جایگزین کنید. **نه migrationهای 001 تا 004، نه Reset و نه `deploy-supabase.bat` را دوباره اجرا نکنید.**

۳. در پوشهٔ اصلی پروژه `npm run build` را اجرا کنید و **در یک پروژهٔ آزمایشی مجزا** ابتدا فایل `supabase/migrations/202609220001_manager_controls.sql` را از ابتدا تا انتها یک‌بار در SQL Editor اجرا کنید. SQL دارای تراکنش `BEGIN/COMMIT` است؛ در صورت خطا متن خطا را نگه دارید و آن را کورکورانه دوباره اجرا نکنید.

۴. در پروژهٔ آزمایشی Edge Function جدید را Deploy و رابط را به API آزمایشی وصل کنید. دست‌کم دو ثبت فروش مشابه و همزمان، ویرایش نقدی/نسیه با وصول قبلی، تخفیف هنگام تسویه، کانتر ۷ رقمی، مرجوعی، موجودی قبل/بعد و داشبورد تاریخی را با دادهٔ ساختگی بررسی کنید. آزمون خودکار کامل و آزمون دیتابیس زنده در محیط تهیهٔ این بسته انجام **نشده** است.

۵. **فقط پس از موفقیت تست و تأیید حسابداری**، برای محیط اصلی بکاپ تازه بگیرید؛ سپس فقط migration جدید را در SQL Editor پروژهٔ اصلی اجرا کنید، Edge API را Deploy کنید و بعد از موفقیت `npm run build` فایل‌های تغییرکرده را Commit و Push کنید. ترتیب SQL → API → Frontend مهم است؛ در طول انتشار ثبت عملیات را متوقف کنید.

## دستورات CMD برای کپی و بیلد در پوشهٔ Git

```
cd /d "C:\Users\aminh\OneDrive\Desktop\Boostan Factory\Boostan_Factory_ERP_v2_Merged_RC1\Boostan_Factory_ERP_Final"
git status --short
npm run build
```

**قبل از کپی:** اگر `git status --short` تغییرات ذخیره‌نشده نشان می‌دهد، از پوشه یک کپی جدا بگیرید؛ فایل‌های موجود را ناخواسته نپوشانید.

**نکتهٔ صداقت آزمون:** در محیط آماده‌سازی، بررسی نحوی فایل‌های تغییرکرده بدون خطا بود، اما نصب وابستگی‌های frontend در محیط بدون دسترسی لازم کامل نشد؛ بنابراین `vite build`، اجرای واقعی PostgreSQL، تست رابط روی موبایل و صحت عددی بهای تمام‌شده هنوز تأیید نشده‌اند.
