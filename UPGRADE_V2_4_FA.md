# اصلاحات فرم فروش و رابط کاربری — نسخه ۲.۴

این بسته روی نسخه Cloud v2.3 نصب می‌شود. فقط فایل‌های رابط کاربری و Edge Function را جایگزین می‌کند؛ **هیچ Migration جدیدی ندارد** و اطلاعات قبلی دیتابیس را حذف یا بازنویسی نمی‌کند.

## تغییرات
- پلاک ایران به صورت چهار کادر مستقل: دو رقم، حرف فارسی، سه رقم و کد شهر دو رقمی؛ حرکت خودکار بین کادرها پس از تکمیل هر بخش. اطلاعات پلاک اختیاری است، اما اگر وارد شود باید کامل باشد.
- شماره تماس مشتری جدید و شماره تماس راننده در فرم فروش، اگر وارد شوند، باید دقیقاً ۱۱ رقم باشند؛ شماره تماس مشتری انتخاب‌شده قبلی دوباره درخواست نمی‌شود. بررسی ۱۱ رقمی در API نیز انجام می‌شود.
- سه‌رقمی شدن نمایش ورودی‌های عددی مبلغ، تعداد و وزن در زمان تایپ، بدون تغییر عدد واقعی ارسال‌شده به سرور؛ شماره‌های تلفن و کدها با جداکننده نمایش داده نمی‌شوند.
- پس‌زمینه بخش نسیه پررنگ‌تر و آیکون تغییر حالت تاریک/روشن کوچک و بدون متن است.
- راهنمای ثبت کانتر: در ابتدای شیفت ثبت شود و به منزله ثبت حضور اپراتور است؛ **قفل زمانی قبلی دوباره فعال نمی‌شود**.
- جلوگیری از ارسال همزمان دوباره فرم فروش از طریق قفل در زمان ارسال.

## استقرار
1. فایل ZIP را در **ریشه همان Repository محلی** استخراج کنید تا مسیرهای `frontend/src/...` و `supabase/functions/erp-api/index.ts` جایگزین شوند.
2. `npm run build` را داخل ریشه پروژه اجرا کنید. اگر Build ناموفق بود، Commit/Push نکنید.
3. چون اعتبارسنجی سمت سرور هم تغییر کرده است، `cloud\deploy-supabase.bat` را اجرا کنید. در این انتشار **هیچ Migration جدیدی نباید نشان داده شود**؛ اگر هر Migration قدیمی را مجدداً پیشنهاد کرد، آن را تأیید نکنید.
4. بعد از موفقیت Deploy ابری و Build محلی، فایل‌های تغییرکرده را بررسی، Commit و روی شاخه `factory-v2-final` Push کنید. وضعیت انتشار را در GitHub Actions چک کنید.

برای آزمایش: در فرم فروش، پلاک `16 ل 785 - 49` را با چهار کادر وارد کنید، با اعداد فارسی و انگلیسی مبالغ بزرگی مثل `۱۲۳۴۵۶۷` بنویسید، شماره ۱۰ رقمی را امتحان کنید (باید خطا بدهد)، مشتری نسیه قبلی را بدون وارد کردن دوباره شماره تماس انتخاب کنید و حالت تاریک/روشن را تغییر دهید.
