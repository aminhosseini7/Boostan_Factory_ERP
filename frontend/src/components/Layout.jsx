import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {useEffect,useState} from 'react';import {useAuth} from '../context/AuthContext';
export default function Layout(){const{user,logout,isManager}=useAuth();const nav=useNavigate();const[dark,setDark]=useState(()=>localStorage.getItem('boostan_theme')==='dark');useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem('boostan_theme',dark?'dark':'light')},[dark]);const managerItems=[['/','داشبورد'],['/sales','فروش'],['/products','محصولات'],['/production','تولید و شیفت‌ها'],['/customers','مشتریان نسیه / وصول مطالبات'],['/inventory','انبار'],['/purchases','خریدها'],['/grinding','آسیاب'],['/expenses','هزینه‌ها'],['/returns','مرجوعی / ابطال'],['/reports','گزارش‌ها و سود'],['/operations-ledger','دیتابیس / دفتر کل'],['/users','کاربران']];const operatorItems=[['/operator','پنل عملیات']];const items=isManager?managerItems:operatorItems;return <div className="app-shell"><aside className="sidebar"><div className="brand"><b>کارخانه بوستان</b><span>سامانه مدیریت کارخانه</span></div><nav>{items.map(([to,label])=><NavLink key={to} to={to} end={to==='/' }>{label}</NavLink>)}</nav><div className="userbox"><div>{user?.fullName||user?.username}</div><small>{isManager?'مدیر':'اپراتور'}</small><button className="ghost" onClick={()=>setDark(v=>!v)}>{dark?'نمایش روشن':'نمایش تاریک'}</button><button className="ghost" onClick={()=>{logout();nav('/login')}}>خروج</button></div></aside><main className="main"><Outlet/></main></div>}
