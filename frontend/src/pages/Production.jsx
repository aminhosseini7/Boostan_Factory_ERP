import {useEffect,useState} from 'react';import api from '../services/api';import {Page,ErrorBox,Empty} from '../components/Ui';
export default function Production(){const [products,setProducts]=useState([]);const [rows,setRows]=useState([]);const [error,setError]=useState(null);const [form,setForm]=useState({productId:'',quantity:'',shift:'MORNING',productionAt:'',note:''});async function load(){try{const [p,r]=await Promise.all([api.get('/products'),api.get('/production')]);setProducts(p.data.filter(x=>x.isActive));setRows(r.data)}catch(e){setError(e)}}useEffect(()=>{load()},[]);async function submit(e){e.preventDefault();try{const body={...form,productionAt:form.productionAt?new Date(form.productionAt).toISOString():undefined};await api.post('/production',body);setForm({productId:'',quantity:'',shift:'MORNING',productionAt:'',note:''});load()}catch(e){setError(e)}}return <Page title="ثبت تولید"><ErrorBox error={error}/><form className="panel form-grid" onSubmit={submit}><label>محصول<select required value={form.productId} onChange={e=>setForm({...form,productId:e.target.value})}><option value="">انتخاب کنید</option>{products.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>مقدار<input required type="number" step="0.001" min="0.001" value={form.quantity} onChange={e=>setForm({...form,quantity:e.target.value})}/></label><label>شیفت<select value={form.shift} onChange={e=>setForm({...form,shift:e.target.value})}><option value="MORNING">صبح</option><option value="EVENING">عصر</option><option value="NIGHT">شب</option></select></label><label>تاریخ/زمان<input type="datetime-local" value={form.productionAt} onChange={e=>setForm({...form,productionAt:e.target.value})}/></label><label className="wide">توضیح<textarea value={form.note} onChange={e=>setForm({...form,note:e.target.value})}/></label><button>ثبت تولید</button></form><div className="panel"><h3>سوابق تولید</h3>{rows.length===0?<Empty/>:<table><thead><tr><th>محصول</th><th>مقدار</th><th>شیفت</th><th>اپراتور</th><th>زمان</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.productName}</td><td>{x.quantity}</td><td>{x.shift}</td><td>{x.operatorName}</td><td>{new Date(x.productionAt).toLocaleString('fa-IR')}</td></tr>)}</tbody></table>}</div></Page>}
