import {useEffect,useState} from 'react';import {LineChart,Line,CartesianGrid,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';import api from '../services/api';import {Page,Card,ErrorBox} from '../components/Ui';
const n=v=>new Intl.NumberFormat('fa-IR').format(Number(v||0));
export default function Dashboard(){const [d,setD]=useState(null);const [error,setError]=useState(null);useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data)).catch(setError)},[]);return <Page title="داشبورد مدیریت"><ErrorBox error={error}/><div className="cards"><Card label="تولید امروز" value={n(d?.todayProduction)}/><Card label="فروش امروز" value={n(d?.todaySales)}/><Card label="کالاهای فعال انبار" value={n(d?.inventoryProductCount)}/><Card label="ارزش موجودی" value={n(d?.inventoryValue)}/><Card label="مطالبات مشتریان" value={n(d?.customerDebt)}/><Card label="کمبود موجودی" value={n(d?.lowStockCount)}/></div><div className="grid2"><div className="panel"><h3>روند تولید ۳۰ روزه</h3><ResponsiveContainer width="100%" height={260}><LineChart data={d?.productionSeries||[]}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip/><Line type="monotone" dataKey="total"/></LineChart></ResponsiveContainer></div><div className="panel"><h3>روند فروش ۳۰ روزه</h3><ResponsiveContainer width="100%" height={260}><LineChart data={d?.salesSeries||[]}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip/><Line type="monotone" dataKey="total"/></LineChart></ResponsiveContainer></div></div><div className="panel"><h3>آخرین فعالیت‌ها</h3><table><thead><tr><th>کاربر</th><th>عملیات</th><th>زمان</th></tr></thead><tbody>{(d?.recentActivities||[]).map(x=><tr key={x.id}><td>{x.userName||'-'}</td><td>{x.action}</td><td>{new Date(x.createdAt).toLocaleString('fa-IR')}</td></tr>)}</tbody></table></div></Page>}
