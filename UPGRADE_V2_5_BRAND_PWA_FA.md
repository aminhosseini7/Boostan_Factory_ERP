# نصب نشان بوستان و قابلیت افزودن روی موبایل

این بسته فقط رابط کاربری است؛ دیتابیس و Edge Function تغییر نمی‌کنند.

- لوگوی تأییدشده انگلیسی در صفحه ورود و نوار کناری؛ نشان همان طرح برای آیکون موبایل.
- عنوان برنامه: کارخانه سبدسازی بوستان.
- فونت Vazirmatn از Google Fonts بارگذاری می‌شود؛ در صورت مسدود بودن سرویس، فونت جایگزین نمایش داده می‌شود. فایل فونت در این بسته وجود ندارد.
- فایل manifest و service worker برای نصب به‌صورت PWA. عملیات تولید و فروش **بدون اینترنت کار نمی‌کند** و برنامه هیچ‌گونه داده مشتری یا احراز هویت را در کش ذخیره نمی‌کند.

## انتشار

۱. بسته را روی ریشه همان Repository شاخه `factory-v2-final` استخراج کن.
۲. `npm run build` و `git status` را اجرا کن.
۳. اگر Build موفق بود، فایل‌های جدید را Commit و Push کن. GitHub Actions سایت را منتشر می‌کند.
۴. روی موبایل نسخه قبلی را به‌روز کن؛ اگر میان‌بر قدیمی داری، آن را حذف کن و از سایت دوباره «افزودن به صفحه اصلی» را بزن.

اگر دستگاه به اینترنت یا دامنه Google Fonts دسترسی نداشته باشد، فونت Vazirmatn دانلود نمی‌شود.
