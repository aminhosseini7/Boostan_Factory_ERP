# اصلاحات قبل از بهره‌برداری — v2.6

- فیلترهای تاریخ جلالی به کادر واضح با قالب `۱۴۰۵/۰۶/۲۸`، جداکننده خودکار و بررسی اعتبار تاریخ تبدیل شده‌اند؛ بازه برعکس هم رد می‌شود.
- کادرهای پلاک با ارقام فارسی/انگلیسی/عربی یکسان کار می‌کنند و بعد از پرشدن، فوکوس به کادر بعدی می‌رود.
- هر کلیک روی «افزودن یک قلم دیگر» فقط یک ردیف با شناسه مستقل می‌سازد؛ دوبار کلیک خیلی سریع نادیده گرفته می‌شود. حذف یک ردیف، سایر ردیف‌ها را حذف نمی‌کند.
- عنوان وضعیت کانترِ منتظر شیفت بعد فارسی شد؛ ردیف‌های عددی اولیه در فرم فروش خالی هستند.
- لوگوی بالای نرم‌افزار کوچک‌تر و گوشه‌های لوگوی ورود گرد شدند.
- پس از ثبت مشتری نسیه جدید، نام و تلفن روی فرم باقی می‌مانند و پیام واضحِ انتخاب مشتری نشان داده می‌شود.
- خطاهای ورودی فروش در کنار همان فیلد نمایش داده می‌شوند، خطاهای ناشناخته نزدیک دکمه ثبت نمایش داده می‌شوند.
- لیست مشتریان نسیه بر اساس مانده بدهی، نزولی مرتب می‌شود؛ نمایش مانده بدهی فقط برای مدیر است.
- آخرین فعالیت‌های داشبورد به ۲۰ مورد محدود شده‌اند.

## بازنشانی داده‌های آزمایشی، جدا از انتشار کد

`cloud/RESET_TEST_DATA_KEEP_PRODUCTS.sql` یک فایل **مستقل** و مخرب است؛ در پوشه migrations قرار ندارد و با deploy خودکار اجرا نمی‌شود. **صرفاً بعد از تهیه و آزمودن فایل پشتیبان و توقف ثبت عملیات، آن را با Supabase SQL Editor روی پروژه درست اجرا کنید.** داده‌های عملیاتی، موجودی فعلی، مشتریان، بدهی‌ها و رسیدها به صفر برمی‌گردند؛ محصولات با شناسه، نام، مشخصات و قیمت فعلی حفظ می‌شوند. کاربران، شیفت‌ها و تنظیمات لازم برنامه نیز باقی می‌مانند. سوابق قیمت‌های آزمایشی با یک رکورد قیمت فعلی برای هر محصول جایگزین می‌شوند.

اگر امروز داده واقعی ثبت شده، این فایل را اجرا نکنید؛ این اسکریپت بدون انتخاب بازه زمانی، **تمام** داده‌های عملیاتی را حذف می‌کند. اجرای SQL را قبل از نسخه‌برداری مجاز ندانید.

پس از بازنشانی، پیش از ثبت اولین تولید/فروش واقعی، مدیر باید شمارش واقعی سبدها و مقدار واقعی مواد اولیه و ضایعات را در صفحه انبار ثبت کند؛ در غیر این صورت موجودی‌های جدید از صفر محاسبه می‌شوند.

انتشار رابط کاربری: Build در شاخه `factory-v2-final`، سپس Commit/Push، بررسی سبز شدن GitHub Actions. تغییر API: `npx supabase functions deploy erp-api --project-ref <YOUR_PROJECT_REF> --use-api`. این نسخه Migration دیتابیس جدید ندارد؛ اسکریپت بازنشانی را در پوشه migrations قرار ندهید.
